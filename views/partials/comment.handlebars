<div class="card w-100 border-right-0 border-left-0 border-bottom-0">
    <div class="card-body">
        <div class=" comment-indent">
            <p>{{this.content}}</p>
            <p>Test</p>
            {{!-- <p class="text-right">{{comment.author.username}}</p> --}}
            <a href="/reviews/{{reviewsId}}/comments/{{comment._id}}/replies/new">Reply</a>
            {{!-- {{#each comment.comments}}
                {{> comment comment=this reviewsId=../postId}}
            {{/each}} --}}
        </div>
    </div>
</div>
